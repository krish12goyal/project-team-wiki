# ?? 4. Data Management & Storage Layer

**Responsibilities:** MongoDB schema, indexing, file system (.md storage), article CRUD consistency, search filtering, and **data migrations**.

**Why:** Handles data correctness + storage strategy. Includes migration services for keeping historical schemas up to date and clean.
